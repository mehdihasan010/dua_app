import 'package:dua/core/config/dua_screen.dart';
import 'package:flutter/material.dart';

class VideoPlayerStyles {
  static double playerHeight = twoHundredFortyPx;
  static double overlayOpacity = 0.5;

  static EdgeInsets controlsPadding = EdgeInsets.symmetric(
    horizontal: sixteenPx,
    vertical: sixPx,
  );

  static EdgeInsets playButtonPadding = EdgeInsets.all(eighteenPx);
}
